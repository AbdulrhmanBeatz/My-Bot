const Discord = require("discord.js");
const client = new Discord.Client();
const sql = require("sqlite");
const moment = require("moment");
client.on('ready', () => {
  console.log(`Logged in as ${client.user.tag}!`);
});

//--------------help command
client.on('message', message => {
  if (message.content === "<>help") {
  let embed = new Discord.RichEmbed()
.setAuthor(message.author.username)
.setTitle("``((يوجد فقط امر ((اكواد``")
.setFooter(`ᵒᶜ|♪мσнαммєɒ♪ ♩`, 'https://cdn.discordapp.com/attachments/402967968557957120/421937743333949440/f61956d4d5f19b9a43f971de03d4600d1.jpg')
.setColor("#0dff00")
.setDescription("🛠تحت الصيانه🛠\nيوجد فقط امر اكواد")
message.channel.sendEmbed(embed);
}
});
//--------------help command
//--------------help command
//--------------help command
//--------------help command
//--------------help command
//--------------help command
//--------------help command
//--------------help command
//--------------help command
//--------------help command
//--------------help command
//--------------help command
//--------------help command
//--------------help command
//--------------help command
//--------------help command
//--------------help command
//--------------help command
//--------------help command
//--------------ferviy








client.on('message', message => {
  if (message.content.startsWith("fire")) {
      if(!message.author.id === '') return;
    if (message.author.bot) return
         message.delete();


               message.channel.send({embed: new Discord.RichEmbed().setTitle('Fire!!🔥').setColor("#FFFF00")}).then(function(m) {
                setTimeout(function() {
                  m.edit({embed: new Discord.RichEmbed().setTitle('Fire!!🔥').setColor("#FFF200")})
                }, 250)
           setTimeout(function() {
            m.edit({embed: new Discord.RichEmbed().setTitle('Fire!!🔥').setColor("#FFE600")})
          }, 250)
           setTimeout(function() {
             m.edit({embed: new Discord.RichEmbed().setTitle('Fire!!🔥').setColor("#FFD900")})
           }, 250)
           setTimeout(function() {
            m.edit({embed: new Discord.RichEmbed().setTitle('Fire!!🔥').setColor("#FFCC00")})
          }, 250)
          setTimeout(function() {
            m.edit({embed: new Discord.RichEmbed().setTitle('Fire!!🔥').setColor("#FFBF00")})
          }, 250)
          setTimeout(function() {
            m.edit({embed: new Discord.RichEmbed().setTitle('Fire!!🔥').setColor("#FFB200")})
          }, 250)
          setTimeout(function() {
            m.edit({embed: new Discord.RichEmbed().setTitle('Fire!!🔥').setColor("#FFA600")})
          }, 250)
          setTimeout(function() {
            m.edit({embed: new Discord.RichEmbed().setTitle('Fire!!🔥').setColor("#FF9900")})
          }, 250)
          setTimeout(function() {
            m.edit({embed: new Discord.RichEmbed().setTitle('Fire!!🔥').setColor("#FF8C00")})
          }, 250)
          setTimeout(function() {
            m.edit({embed: new Discord.RichEmbed().setTitle('Fire!!🔥').setColor("#FF8000")})
          }, 250)
          setTimeout(function() {
            m.edit({embed: new Discord.RichEmbed().setTitle('Fire!!🔥').setColor("#FF7300")})
          }, 250)
          setTimeout(function() {
            m.edit({embed: new Discord.RichEmbed().setTitle('Fire!!🔥').setColor("#FF6600")})
          }, 250)
          setTimeout(function() {
            m.edit({embed: new Discord.RichEmbed().setTitle('Fire!!🔥').setColor("#FF5900")})
          }, 250)
          setTimeout(function() {
            m.edit({embed: new Discord.RichEmbed().setTitle('Fire!!🔥').setColor("#FF4C00")})
          }, 250)
          setTimeout(function() {
            m.edit({embed: new Discord.RichEmbed().setTitle('Fire!!🔥').setColor("#FF4000")})
          }, 250)
          setTimeout(function() {
            m.edit({embed: new Discord.RichEmbed().setTitle('Fire!!🔥').setColor("#FF3300")})
          }, 250)
          setTimeout(function() {
            m.edit({embed: new Discord.RichEmbed().setTitle('Fire!!🔥').setColor("#FF2600")})
          }, 250)
          setTimeout(function() {
            m.edit({embed: new Discord.RichEmbed().setTitle('Fire!!🔥').setColor("#FF1A00")})
          }, 250)
          setTimeout(function() {
            m.edit({embed: new Discord.RichEmbed().setTitle('Fire!!🔥').setColor("#FF0D00")})
          }, 250)
          setTimeout(function() {
            m.edit({embed: new Discord.RichEmbed().setTitle('Fire!!🔥').setColor("#FF0000")})
          }, 250)
         });
       }
})


client.on('message', message => {
  if (message.content.startsWith("water")) {
      if(!message.author.id === '') return;
    if (message.author.bot) return
         message.delete();


               message.channel.send({embed: new Discord.RichEmbed().setTitle('Water!!💧').setColor("#00FFFF")}).then(function(m) {
                setTimeout(function() {
                  m.edit({embed: new Discord.RichEmbed().setTitle('Water!!💧').setColor("#00F2FF")})
                }, 250)
           setTimeout(function() {
            m.edit({embed: new Discord.RichEmbed().setTitle('Water!!💧').setColor("#00E6FF")})
          }, 250)
           setTimeout(function() {
             m.edit({embed: new Discord.RichEmbed().setTitle('Water!!💧').setColor("#00D9FF")})
           }, 250)
           setTimeout(function() {
            m.edit({embed: new Discord.RichEmbed().setTitle('Water!!💧').setColor("#00CCFF")})
          }, 250)
          setTimeout(function() {
            m.edit({embed: new Discord.RichEmbed().setTitle('Water!!💧').setColor("#00BFFF")})
          }, 250)
          setTimeout(function() {
            m.edit({embed: new Discord.RichEmbed().setTitle('Water!!💧').setColor("#00B2FF")})
          }, 250)
          setTimeout(function() {
            m.edit({embed: new Discord.RichEmbed().setTitle('Water!!💧').setColor("#00A6FF")})
          }, 250)
          setTimeout(function() {
            m.edit({embed: new Discord.RichEmbed().setTitle('Water!!💧').setColor("#0099FF")})
          }, 250)
          setTimeout(function() {
            m.edit({embed: new Discord.RichEmbed().setTitle('Water!!💧').setColor("#008CFF")})
          }, 250)
          setTimeout(function() {
            m.edit({embed: new Discord.RichEmbed().setTitle('Water!!💧').setColor("#0080FF")})
          }, 250)
          setTimeout(function() {
            m.edit({embed: new Discord.RichEmbed().setTitle('Water!!💧').setColor("#0073FF")})
          }, 250)
          setTimeout(function() {
            m.edit({embed: new Discord.RichEmbed().setTitle('Water!!💧').setColor("#0066FF")})
          }, 250)
          setTimeout(function() {
            m.edit({embed: new Discord.RichEmbed().setTitle('Water!!💧').setColor("#0059FF")})
          }, 250)
          setTimeout(function() {
            m.edit({embed: new Discord.RichEmbed().setTitle('Water!!💧').setColor("#004DFF")})
          }, 250)
          setTimeout(function() {
            m.edit({embed: new Discord.RichEmbed().setTitle('Water!!💧').setColor("#0040FF")})
          }, 250)
          setTimeout(function() {
            m.edit({embed: new Discord.RichEmbed().setTitle('Water!!💧').setColor("#0033FF")})
          }, 250)
          setTimeout(function() {
            m.edit({embed: new Discord.RichEmbed().setTitle('Water!!💧').setColor("#0026FF")})
          }, 250)
          setTimeout(function() {
            m.edit({embed: new Discord.RichEmbed().setTitle('Water!!💧').setColor("#0019FF")})
          }, 250)
          setTimeout(function() {
            m.edit({embed: new Discord.RichEmbed().setTitle('Water!!💧').setColor("#000DFF")})
          }, 250)
          setTimeout(function() {
            m.edit({embed: new Discord.RichEmbed().setTitle('Water!!💧').setColor("#0000FF")})
          }, 250)
         });
       }
})









   client.on('message', message => {
    if (message.content.startsWith("$stats")) {
    message.channel.send({
        embed: new Discord.RichEmbed()
            .setColor('RANDOM')
            .setTitle('Stats Bot / Info ')
            .addField('``Uptime``', timeCon(process.uptime()), true)
            .addField('``Ping Is``' , `${Date.now() - message.createdTimestamp}` + '``Ms``', true)
            .addField('``RAM Usage``', `${(process.memoryUsage().rss / 1048576).toFixed()}MB`, true)
            .addField('``Guild Count``', client.guilds.size, true)
            .addField('``Bot In channel``' , `${client.channels.size}` , true)
            .addField('``Users rout``' ,`${client.users.size}` , true)
            .addField('``Name Bot Or tag``' , `${client.user.tag}` , true)
            .addField('``Bot Id``' , `${client.user.id}` , true)})}});
  function timeCon(time) {
    let days = Math.floor(time % 31536000 / 86400)
    let hours = Math.floor(time % 31536000 % 86400 / 3600)
    let minutes = Math.floor(time % 31536000 % 86400 % 3600 / 60)
    let seconds = Math.round(time % 31536000 % 86400 % 3600 % 60)
    days = days > 9 ? days : '0' + days
    hours = hours > 9 ? hours : '0' + hours
    minutes = minutes > 9 ? minutes : '0' + minutes
    seconds = seconds > 9 ? seconds : '0' + seconds
    return `${days > 0 ? `${days}:` : ''}${(hours || days) > 0 ? `${hours}:` : ''}${minutes}:${seconds}`
  }





















var prefix = "<>"
client.on('message', message => {
  if (message.author.id === client.user.id) return;
  if (message.guild) {
 let embed = new Discord.RichEmbed()
  let args = message.content.split(' ').slice(1).join(' ');
if(message.content.split(' ')[0] == prefix + 'bc') {
  if (!args[1]) {
message.channel.send("**.bc <message>**");
return;
}
      message.guild.members.forEach(m => {
 if(!message.member.hasPermission('ADMINISTRATOR')) return;
          var bc = new Discord.RichEmbed()
          .setAuthor(message.author.username, message.author.avatarURL)
          .addField('** الـسيرفر**', `${message.guild.name}`,true)
          .addField(' **الـمرسل **', `${message.author.username}#${message.author.discriminator}`,true)
          .addField(' **الرسالة** ', args)
          .setThumbnail(message.guild.iconURL)
          .setColor('RANDOM')
          m.send(`${m}`,{embed: bc});
      });
      const AziRo = new Discord.RichEmbed()
      .setAuthor(message.author.username, message.author.avatarURL)
      .setTitle('✔️ | جاري ارسال رسالتك ')
      .addBlankField(true)
      .addField('👥 | عدد الاعضاء المرسل لهم ', message.guild.memberCount , true)
      .addField('📋| الرسالة ', args)
      .setColor('RANDOM')
      message.channel.sendEmbed(AziRo);
  }
  } else {
      return;
  }
});









client.on('message', message => {
  if(message.content.startsWith (prefix  + 'mb')) {
      if(!message.channel.guild) return;
    let embed = new Discord.RichEmbed()
    .setColor('RANDOM')
          .setThumbnail(message.author.avatarURL)
      .setFooter(message.author.username, message.author.avatarURL)

    .setDescription(`**:battery: حالة اعضاء السيرفر**

**:green_heart: Online**  **[ ${message.guild.members.filter(m=>m.presence.status == 'online').size} ]**
**:yellow_heart: Idle**       **[ ${message.guild.members.filter(m=>m.presence.status == 'idle').size} ]**
**:heart: DND**     **[ ${message.guild.members.filter(m=>m.presence.status == 'dnd').size} ]**
**:black_heart: Offline** **[ ${message.guild.members.filter(m=>m.presence.status == 'offline').size} ]** `)
.setDescription(`**👥members** **[${message.guild.memberCount}]**`)

        message.channel.send()

message.channel.sendEmbed(embed)
}
});






































client.on('message', message => {
  var prefix = "<>";

      if (message.author.id === client.user.id) return;
      if (message.guild) {
     let embed = new Discord.RichEmbed()
      let args = message.content.split(' ').slice(1).join(' ');
  if(message.content.split(' ')[0] == prefix + 'bc2') {
      if (!args[1]) {
  message.channel.send("**$bc2 <message>**");
  return;
  }
          message.guild.members.forEach(m => {
     if(!message.member.hasPermission('ADMINISTRATOR')) return;
              var bc = new Discord.RichEmbed()
              .addField('» السيرفر :', `${message.guild.name}`)
              .addField('» المرسل : ', `${message.author.username}#${message.author.discriminator}`)
              .addField(' » الرسالة : ', args)
              .setColor('#ff0000')
              // m.send(`[${m}]`);
              m.send(`${m}`,{embed: bc});
          });
      }
      } else {
          return;
      }
  });






































  client.on('message', message => {
    if (message.content === "<>server") {
    if(!message.channel.guild) return;
    const millis = new Date().getTime() - message.guild.createdAt.getTime();
    const now = new Date();

    const verificationLevels = ['None', 'Low', 'Medium', 'Insane', 'Extreme'];
    const days = millis / 1000 / 60 / 60 / 24;
    let roles = client.guilds.get(message.guild.id).roles.map(r => r.name);
    var embed  = new Discord.RichEmbed()
    .setAuthor(message.guild.name, message.guild.iconURL)
    .addField("**🆔 سيـرفر ايــدي**", "**"+message.guild.id+"**",true)
    .addField("**👑 سيــرفر اونـر**", "**"+message.guild.owner+"**" ,true)
    .addField("**✅ الشــات الاســاســي**" , "**"+message.guild.DefaultChannel+"**" ,true)
    .addField("**🌍 المـوقع**" , "**"+message.guild.region+"**",true)
    .addField('**💬 عدد الرومــات الكتابيــة**',`**[ ${message.guild.channels.filter(m => m.type === 'text').size} ] Channel **`,true)
    .addField("**📣 عدد الرومــات الصوتــية**", ` ** [ ${message.guild.channels.filter(m => m.type === 'voice').size} ] Channel ** `,true)
    .addField("**🤔 عدد ايــام انشــاء السيــرفر**", ` ** [ ${days.toFixed(0)} ] ** Day ` ,true)
    .addField("**🏅 الــرتــب**",`**[${message.guild.roles.size}]** Role `,true)
    .addField("**💠 مســتوى حمــاية الســيرفر**", ` ** [ ${verificationLevels[message.guild.verificationLevel]} ] ** `,true)
    .addField("**🤗 ايموجي السيرفر**",`**[${message.guild.emojis.size}]** emoji `,true)
    .addField("**👥عدد الاعضــاء**",`**${message.guild.memberCount}**`)
    .setThumbnail(message.guild.iconURL)
    .setColor('#00ffff')
    message.channel.sendEmbed(embed)

    }
    });





































    client.on("message", message => {
      const prefix = "<>"

            if(!message.channel.guild) return;
     if(message.author.bot) return;
        if(message.content === prefix + "as"){
            const embed = new Discord.RichEmbed()

        .setTitle(`This is  ** ${message.guild.name} **  Photo !`)
    .setAuthor(message.author.username, message.guild.iconrURL)
      .setColor(0x164fe3)
      .setImage(message.guild.iconURL)
      .setURL(message.guild.iconrURL)
                      .setTimestamp()

     message.channel.send({embed});
        }
    });






























    client.on('ready', () => {
      console.log(`Logged in as ${client.user.tag} !`);
            client.user.setActivity("Official Coding",{type: 'watching'});

    });




    client.on('message', message => {
      if (message.content.startsWith("<>av")) {
          var mentionned = message.mentions.users.first();
      var official;
        if(mentionned){
            var official = mentionned;
        } else {
            var official = message.author;

        }
          const embed = new Discord.RichEmbed()
          .setColor("RANDOM")
          .setImage(`${official.avatarURL}`)
        message.channel.sendEmbed(embed);
      }
  });











client.on("message", (message) => {
  if (message.content.startsWith("<>ct")) {
              if (!message.member.hasPermission('MANAGE_CHANNELS')) return message.reply(" `MANAGE_CHANNELS`:laughing::laughing:ليس لديك صلاحية:laughing::laughing: ");
          let args = message.content.split(" ").slice(1);
      message.guild.createChannel(args.join(' '), 'text');
  message.channel.sendMessage('📝🚪✅تـم إنـشاء روم كـتابـي')

  }
  });




















  client.on("message", (message) => {
  if (message.content.startsWith("<>cv")) {
              if (!message.member.hasPermission('MANAGE_CHANNELS')) return message.reply(" `MANAGE_CHANNELS` :laughing::laughing:ليس لديك صلاحية:laughing::laughing: ");
          let args = message.content.split(" ").slice(1);
      message.guild.createChannel(args.join(' '), 'voice');
      message.channel.sendMessage('🔊🚪✅ تـم إنـشاء روم صـوتي')

  }
  });

























  client.on("message", (message) => {
      if (message.content.startsWith('<>de')) {
          if (!message.member.hasPermission('MANAGE_CHANNELS')) return message.reply(" `MANAGE_CHANNELS` :laughing::laughing:ليس لديك صلاحية:laughing::laughing: ");

          let args = message.content.split(' ').slice(1);
          let channel = message.client.channels.find('name', args.join(' '));
          if (!channel) return message.reply('**There is no room like this name -_-**').catch(console.error);
          channel.delete()
          message.channel.sendMessage('❌✏️تم الحذف')
      }
  });
  client.on("channeldelete", (channel) => {
    if (!channel.guild.channels.find("name","log")) return;
      var atacc = new Discord.RichEmbed(atacc);
      atacc.setColor("RANDOM")
      atacc.setDescription('The ' + channel + ' ' + `**(` + channel.name + `)**` + ' channel was created')
      atacc.setAuthor(' CHANNEL CREATED ')
      atacc.setFooter('ID: ' + channel.id + " | " + channel.createdTimestamp)
      channel.guild.channels.find("name","log").sendEmbed(atacc)
    });




















  client.on("message", msg => {
    var prefix = "<>";
    if(msg.content.startsWith (prefix + ".....")) {
      var mentionned = msg.mentions.users.first();
      var official;
        if(mentionned){
            var official = mentionned;}
            else
        {var official = msg.author;}
        var roles = client.guilds.get(message.guild.id).roles.map(r => r.name);
    if(!msg.channel.guild) return msg.reply("**:x: اسف لكن هذا الامر للسيرفرات فقط **");
    const embed = new Discord.RichEmbed();
    embed.addField(":cloud_tornado:  الاسم", `**[ ${official.username}#${official.discriminator} ]**`, true)
    .addField(":id:  الايدي", `**[ ${official.id} ]**`, true)
    .setColor("RANDOM")
    .setFooter(official.username , official.avatarURL)
    .setThumbnail(`${official.avatarURL}`).setTimestamp()
    .setURL(`${official.avatarURL}`)
    .addField(":spy:  الحالة", `**[ ${official.presence.status.toUpperCase()} ]**`, true)
    .addField(":satellite_orbital:   يلعب", `**[ ${official.presence.game === null ? "No Game" : official.presence.game.name} ]**`, true)
    .addField(":military_medal:  الرتب", `**[ ${message.guild.roles.size} ]**`, true)
    .addField(":robot:  هل هو بوت", `**[ ${official.bot.toString().toUpperCase()} ]**`, true);
    msg.channel.send({embed: embed})}});




  client.on("message", message => {
    if (message.author.bot) return;

    let command = message.content.split(" ")[0];

    if (command === "<>mu") {
          if (!message.member.hasPermission('MANAGE_ROLES')) return message.reply("** :laughing::laughing:ليس لديك صلاحية:laughing::laughing:'Manage Roles' **").catch(console.error);
    let user = message.mentions.users.first();
    let modlog = client.channels.find('name', 'mute-log');
    let muteRole = client.guilds.get(message.guild.id).roles.find('name', 'Muted');
    if (!muteRole) return message.reply("** اصنع رتبة 'Muted' **").catch(console.error);
    if (message.mentions.users.size < 1) return message.reply('** يجب عليك منشنت شخص اولاً**').catch(console.error);

    const embed = new Discord.RichEmbed()
      .setColor(0x00AE86)
      .setTimestamp()
      .addField('الأستعمال:', 'اسكت/احكي')
      .addField('تم ميوت:', `${user.username}#${user.discriminator} (${user.id})`)
      .addField('بواسطة:', `${message.author.username}#${message.author.discriminator}`)

     if (!message.guild.member(client.user).hasPermission('MANAGE_ROLES_OR_PERMISSIONS')) return message.reply('** :cry::cry:هذه الصلاحيه ليست لدي:cry::cry: Manage Roles **').catch(console.error);

    if (message.guild.member(user).roles.has(muteRole.id)) {
  return message.reply("**:white_check_mark: 🤐 تم اعطاء العضو ميوت**").catch(console.error);
  } else {
      message.guild.member(user).addRole(muteRole).then(() => {
  return message.reply("**:white_check_mark: 🤐 تم اعطاء العضو ميوت كتابي**").catch(console.error);
  });
    }

  };

  });



















  client.on("message", message => {
    if (message.author.bot) return;

    let command = message.content.split(" ")[0];

    if (command === "<>unmu") {
          if (!message.member.hasPermission('MANAGE_ROLES')) return message.reply("** :laughing::laughing:ليس لديك صلاحية:laughing::laughing:'Manage Roles' **").catch(console.error);
    let user = message.mentions.users.first();
    let modlog = client.channels.find('name', 'mute-log');
    let muteRole = client.guilds.get(message.guild.id).roles.find('name', 'Muted');
    if (!muteRole) return message.reply("** اصنع 'Muted' **").catch(console.error);
    if (message.mentions.users.size < 1) return message.reply('** يجب عليك منشنت شخص اولاً**').catch(console.error);
    const embed = new Discord.RichEmbed()
      .setColor(0x00AE86)
      .setTimestamp()
      .addField('الأستعمال:', 'اسكت/احكي')
      .addField('تم فك الميوت عن:', `${user.username}#${user.discriminator} (${user.id})`)
      .addField('بواسطة:', `${message.author.username}#${message.author.discriminator}`)

    if (!message.guild.member(client.user).hasPermission('MANAGE_ROLES_OR_PERMISSIONS')) return message.reply('**:cry::cry:هذه الصلاحيه ليست لدي:cry::cry: Manage Roles **').catch(console.error);

    if (message.guild.member(user).removeRole(muteRole.id)) {
  return message.reply("**:white_check_mark: .. تم فك الميوت عن الشخص **").catch(console.error);
  } else {
      message.guild.member(user).removeRole(muteRole).then(() => {
  return message.reply("**:white_check_mark: .. تم فك الميوت عن الشخص **").catch(console.error);
  });
    }

  };

  });




















  client.on('message', message => {

    if(message.content === prefix + "much") {
                        if(!message.channel.guild) return message.reply('** This command only for servers**');

if(!message.member.hasPermission('MANAGE_MESSAGES')) return message.reply(' **__:laughing::laughing:ليس لديك صلاحيات:laughing::laughing:__**');
           message.channel.overwritePermissions(message.guild.id, {
         SEND_MESSAGES: false

           }).then(() => {
               message.reply("**__تم تقفيل الشات__ :white_check_mark: **")
           });
             }
 if(message.content === prefix + "unmuch") {
                     if(!message.channel.guild) return message.reply('** This command only for servers**');

if(!message.member.hasPermission('MANAGE_MESSAGES')) return message.reply('**__:laughing::laughing:ليس لديك صلاحيات:laughing::laughing:__**');
           message.channel.overwritePermissions(message.guild.id, {
         SEND_MESSAGES: true

           }).then(() => {
               message.reply("**__تم فتح الشات__:white_check_mark:**")
           });
             }



});




















var prefix = "<>"
client.on('message', message => {
  if (message.author.x5bz) return;
  if (!message.content.startsWith(prefix)) return;

  let command = message.content.split(" ")[0];
  command = command.slice(prefix.length);

  let args = message.content.split(" ").slice(1);

  if (command == "kick") {
               if(!message.channel.guild) return message.reply('** This command only for servers**');

  if(!message.guild.member(message.author).hasPermission("KICK_MEMBERS")) return message.reply("**` KICK_MEMBERS `:laughing::laughing:ليس لديك صلاحية:laughing::laughing:**");
  if(!message.guild.member(client.user).hasPermission("KICK_MEMBERS")) return message.reply("** ` KICK_MEMBERS `:cry::cry:هذه الصلاحيه ليست لدي:cry::cry:**");
  let user = message.mentions.users.first();
  let reason = message.content.split(" ").slice(2).join(" ");
  if (message.mentions.users.size < 1) return message.reply("**منشن الشخص**");
  if(!reason) return message.reply ("**اكتب سبب الطرد**");
  if (!message.guild.member(user)
  .kickable) return message.reply("**لايمكنني طرد شخص اعلى من رتبتي يرجه اعطاء البوت رتبه عاليه**");

  message.guild.member(user).kick();

  const kickembed = new Discord.RichEmbed()
  .setAuthor(`KICKED!`, user.displayAvatarURL)
  .setColor("RANDOM")
  .setTimestamp()
  .addField("**User:**",  '**[ ' + `${user.tag}` + ' ]**')
  .addField("**By:**", '**[ ' + `${message.author.tag}` + ' ]**')
  .addField("**Reason:**", '**[ ' + `${reason}` + ' ]**')
  message.channel.send({
    embed : kickembed
  })
}
});
























var prefix = "<>"
client.on('message', message => {
  if (message.author.x5bz) return;
  if (!message.content.startsWith(prefix)) return;

  let command = message.content.split(" ")[0];
  command = command.slice(prefix.length);

  let args = message.content.split(" ").slice(1);

  if (command == "ban") {
               if(!message.channel.guild) return message.reply('** This command only for servers**');

  if(!message.guild.member(message.author).hasPermission("BAN_MEMBERS")) return message.reply("**` BAN_MEMBERS `:laughing::laughing:ليس لديك صلاحية:laughing::laughing:**");
  if(!message.guild.member(client.user).hasPermission("BAN_MEMBERS")) return message.reply("**` BAN_MEMBERS :cry::cry:هذه الصلاحيه ليست لدي:cry::cry:`**");
  let user = message.mentions.users.first();
  let reason = message.content.split(" ").slice(2).join(" ");
  if (message.mentions.users.size < 1) return message.reply("**منشن الشخص**");
  if(!reason) return message.reply ("**اكتب سبب الطرد**");
  if (!message.guild.member(user)
  .bannable) return message.reply("**لايمكنني طرد شخص اعلى من رتبتي يرجه اعطاء البوت رتبه عاليه**");

  message.guild.member(user).ban(7, user);

  const banembed = new Discord.RichEmbed()
  .setAuthor(`BANNED!`, user.displayAvatarURL)
  .setColor("RANDOM")
  .setTimestamp()
  .addField("**User:**",  '**[ ' + `${user.tag}` + ' ]**')
  .addField("**By:**", '**[ ' + `${message.author.tag}` + ' ]**')
  .addField("**Reason:**", '**[ ' + `${reason}` + ' ]**')
  message.channel.send({
    embed : banembed
  })
}
});




client.on('message', message => {
  if(message.content === "<>bot") {
      const embed = new Discord.RichEmbed()
      .setColor("#00FFFF")
      .setThumbnail(client.user.avatarURL)
      .setDescription(`**Servers**🌐 **__${client.guilds.size}__**
**Users**👥 **__${client.users.size}__**
**Channels**📚 **__${client.channels.size}__** `)
             message.channel.sendEmbed(embed);
         }
});
























client.on('ready',  () => {
console.log('started𝓞𝓕𝓕𝓘𝓒𝓘𝓐𝓛 𝓒𝓞𝓓𝓘𝓝𝓖');
console.log(`Logged in as * [ " ${client.user.username} " ] servers! [ " ${client.guilds.size} " ]`);
console.log(`Logged in as * [ " ${client.user.username} " ] Users! [ " ${client.users.size} " ]`);
console.log(`Logged in as * [ " ${client.user.username} " ] channels! [ " ${client.channels.size} " ]`);
console.log(`|=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=|`);
console.log(`|                                                           |`);
console.log(`|                                                           |`);
console.log(`|                                                           |`);
console.log(`|                                                           |`);
console.log(`|                       SHYBOY_05                           |`);
console.log(`|                                                           |`);
console.log(`|                                                           |`);
console.log(`|                                                           |`);
console.log(`|                                                           |`);
console.log(`|--=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=|`);
});






























   client.on('message', function(message) {
    if (message.channel.type === "dm") {
        if (message.author.id === client.user.id) return;
        var iiMo = new Discord.RichEmbed()
        .setColor('RANDOM')
        .setTimestamp()
        .setTitle('**جاني خاص**')
        .setThumbnail(`${message.author.avatarURL}`)
        .setDescription(`\n\n\`\`\`${message.content}\`\`\``)
        .addField(`**اسمه:arrow_down:**`,`\n**${message.author.tag}**\n**الايدي حقه:arrow_down: \n${message.author.id}**`)
    client.channels.get("422358124402573314").send({embed:iiMo});
    }
});















var prefix = "<>";

client.on('message', message => {
  if (message.author.bot) return;
  if (!message.content.startsWith(prefix)) return;

  let command = message.content.split(" ")[0];
  command = command.slice(prefix.length);

  let args = message.content.split(" ").slice(1);

if (command == "say") {
    let say = new Discord.RichEmbed()
    .setDescription(args.join("  "))
    .setColor(0x23b2d6)
    message.channel.sendEmbed(say);
    message.delete();
  }


});


































client.on("message", message => {
  if (message.content === "<>ping") {
  const embed = new Discord.RichEmbed()
.setColor("RANDOM")
.addField('📶**Ping:**📶' , `${Date.now() - message.createdTimestamp}` + ' ms')
message.channel.sendEmbed(embed);
}
});
























var prefix = "<>"
client.on('message', message => {
    if(message.content.startsWith (prefix  + 'roles')) {
      if(!message.channel.guild) return message.reply('**:x: اسف لكن هذا الامر للسيرفرات فقط **');
        var roles = message.guild.roles.map(roles => `${roles.name}, `).join(' ')
        const embed = new Discord.RichEmbed()
        .setColor('#FFFF00')
        .addField(':trophy:  عدد الرتب',`**[ ${message.guild.roles.size} ]**`)
        message.channel.sendEmbed(embed);
    }
  });















var prefix = "<>";

client.on("message", message => {

            if (message.content.startsWith(prefix + "bc3")) {
                         if (!message.member.hasPermission("ADMINISTRATOR"))  return;
  let args = message.content.split(" ").slice(1);
  var argresult = args.join(' ');
  message.guild.members.filter(m => m.presence.status !== 'offline').forEach(m => {
 m.send(`${argresult}\n ${m}`);
})
 message.channel.send(`\`${message.guild.members.filter(m => m.presence.status !== 'online').size}\` : عدد الاعضاء المستلمين`);
 message.delete();
};
});












    var prefix = "<>"
client.on('message', message => {
  if (message.content.startsWith(prefix + 'cl')) {
    if(!message.channel.guild) return message.reply('**:x: فقط Official Coding اسف لكن هذا الامر لسيرفر  **\nhttps://discord.gg/dxgKSbR');
    if (!message.member.hasPermission('MANAGE_MESSAGES')) return message.reply(`ماعندك هذا البرمشن[*MANAGE_MESSAGES*] `).catch(console.error);
message.delete()
if(!message.channel.guild) return;
let args = message.content.split(" ").slice(1);

const messagecount = parseInt(args.join(' '));

message.channel.fetchMessages({

limit: messagecount

}).then(messages => message.channel.bulkDelete(messages));
message.channel.sendMessage("", {embed: {
  title: "``✏️✅ تــم مسح الشات ``",
  color: 0x06DF00,
  footer: {

  }
  }}).then(msg => {msg.delete(3000)});
};

});











client.on('guildMemberAdd', member=> {
    member.addRole(member.guild.roles.find("name","ᵒᶜ | coder"));
    });




    client.on('message', message => {
      if (message.content === "<>roles") {
        if(!message.channel.guild) return message.reply('**:x: فقط Official Coding اسف لكن هذا الامر لسيرفر  **\nhttps://discord.gg/dxgKSbR');
          var roles = message.guild.roles.map(roles => `${roles.name}, `).join(' ')
          const embed = new Discord.RichEmbed()
          .setColor('RANDOM')
          .addField('Roles:',`\n**${roles}**`)
          message.channel.sendEmbed(embed);
      }
  });







 client.on("message", message => {
  if (message.content === "اكواد") {
    if(!message.channel.guild) return message.reply('**:x: فقط Official Coding اسف لكن هذا الامر لسيرفر  **\nhttps://discord.gg/dxgKSbR');
   const embed = new Discord.RichEmbed()
.setColor("#000dff")
.setDescription('**__⬇️✅هنا قائمة الأكواد✅⬇️__**\n\n**1-معلوماتي**\n\n**2-كود سيرفر**\n\n**3-كود بوت**\n\n**4-حالة الاعضاء**\n\n**5-كود سرعة البوت**\n\n**6-كود صورتي**\n\n**7-كود صورة السيرفر**\n\n**8-كود قول**\n\n**9-كود الوقت**\n\n**10-كود معلومات عن البوت**\n\n**11-كود قرعة**\n\n**12-هيلب في الشات**\n\n**13-هيلب في الخاص**\n\n**14-كود العاب**\n\n**15-كود طرد مع السبب**')
message.channel.sendEmbed(embed)
 }
 });
 client.on("message", message => {
  if (message.content === "معلوماتي") {
    if(!message.channel.guild) return message.reply('**:x: فقط Official Coding اسف لكن هذا الامر لسيرفر  **\nhttps://discord.gg/dxgKSbR');
   const embed = new Discord.RichEmbed()
.setColor("#80ff00")
.setDescription('__**كود ايدي يعطيك معلومات عن الحساب**__\n```js\nclient.on("message", msg => {\nvar prefix = "البريفكس";\nif(msg.content.startsWith ("معلوماتي")) {\nif(!msg.channel.guild) return msg.reply("**:x: اسف لكن هذا الامر للسيرفرات فقط **");     \nconst embed = new Discord.RichEmbed();\nembed.addField(":cloud_tornado:  الاسم", `**[ ${msg.author.username}#${msg.author.discriminator} ]**`, true)\n.addField(":id:  الايدي", `**[ ${msg.author.id} ]**`, true)\n.setColor("RANDOM")\n.setFooter(msg.author.username , msg.author.avatarURL)\n.setThumbnail(`${msg.author.avatarURL}`).setTimestamp()\n.setURL(`${msg.author.avatarURL}`)\n.addField(":spy:  الحالة", `**[ ${msg.author.presence.status.toUpperCase()} ]**`, true)\n.addField(":satellite_orbital:   يلعب", `**[ ${msg.author.presence.game === null ? "No Game" : msg.author.presence.game.name} ]**`, true)\n.addField(":military_medal:  الرتب", `**[ ${msg.member.roles.filter(r => r.name).size} ]**`, true)\n.addField(":robot:  هل هو بوت", `**[ ${msg.author.bot.toString().toUpperCase()} ]**`, true);\nmsg.channel.send({embed: embed})}});```')
message.channel.sendEmbed(embed)
 }
 });
 client.on("message", message => {
  if (message.content === "كود سيرفر") {
    if(!message.channel.guild) return message.reply('**:x: فقط Official Coding اسف لكن هذا الامر لسيرفر  **\nhttps://discord.gg/dxgKSbR');
   const embed = new Discord.RichEmbed()
.setColor("#df3703")
.setDescription('__**كود معلومات عن السيرفر**__\n```js\nclient.on("message", message => {\nif (message.content === "سيرفر") {\nif (!message.channel.guild) returnverificationLevel = message.guild.verificationLevel;\nconst verificationLevels = ["None","Low","Meduim","High","Extreme"];var Y1 = message.guild.createdAt.getFullYear() - 2000\nvar M2 = message.guild.createdAt.getMonth()\nvar D3 = message.guild.createdAt.getDate()\nconst xNiTRoZ = new Discord.RichEmbed()\n.setAuthor(message.author.username , message.author.avatarURL)\n.setColor("RANDOM").setTimestamp()\n.setTitle(message.guild.name,message.guild.iconURL)\n.addField(":crown: اونر السيرفر",`${message.guild.owner.user.username}#${message.guild.owner.user.discriminator}`)\n.addField(":id: اي دي السيرفر",`${message.guild.id}`,true)\n.addField(":date: أنشئت في",D3 + "." + M2 + "." + Y1,true)                 \n.addField(":busts_in_silhouette: الاعضاء " + ` ${message.guild.memberCount} `,"Online "+`[ ${message.guild.members.filter(m=>m.presence.status == "online","idle","dnd").size} ]`+ ","+"Offline "+`[ ${message.guild.members.filter(m=>m.presence.status == "offline").size} ]`,true)\n.addField(":speech_balloon: قنوات" +" "+message.guild.channels.size+" ",`Text [ ${message.guild.channels.filter(m => m.type === "text").size} ]`+", "+`Voice [ ${message.guild.channels.filter(m => m.type === "voice").size} ]`,true)\n.addField(":earth_asia: الدوله",message.guild.region)\n.addField(":ribbon: ايموجي السيرفر",`${message.guild.emojis.size}`,true).addField(":construction: مستوى التحقق",`${verificationLevels[message.guild.verificationLevel]}`,true).addField("🏆 الرتب  "+message.guild.roles.size+" ","Type`-roles` To See The Server Roles!")\n message.channel.send({embed:xNiTRoZ});}});```')
message.channel.sendEmbed(embed)
 }
 });
 client.on("message", message => {
  if (message.content === "كود بوت") {
    if(!message.channel.guild) return message.reply('**:x: فقط Official Coding اسف لكن هذا الامر لسيرفر  **\nhttps://discord.gg/dxgKSbR');
   const embed = new Discord.RichEmbed()
.setColor("#80ff00")
.setDescription('__**كود معلومات عن البوت في كم سيرفر وكم روم وكم شخص**__\n```js\nclient.on("message", message => {\nif(message.content === "بوت") {\nconst embed = new Discord.RichEmbed()\n.setColor("#00FFFF")\n.setDescription(`**Servers**🌐 **__${client.guilds.size}__**\n**Users**👥 **__${client.users.size}__**\n**Channels**📚 **__${client.channels.size}__** `)\nmessage.channel.sendEmbed(embed);}});```')
message.channel.sendEmbed(embed)
 }
 });
client.on("message", message => {
  if (message.content === "حالة الاعضاء") {
    if(!message.channel.guild) return message.reply('**:x: فقط Official Coding اسف لكن هذا الامر لسيرفر  **\nhttps://discord.gg/dxgKSbR');
   const embed = new Discord.RichEmbed()
.setColor("#80ff00")
.setDescription('__**كود حالة اعضاء السيرفر**__\n```js\nclient.on("message", message => {\nif (message.content.startsWith("حالة الاعضاء")) \nif (message.author.bot) return;{\nif (!message.channel.guild) return;\nlet embed = new Discord.RichEmbed()\n.setColor("RANDOM")\n.setThumbnail(message.author.avatarURL)\n.setFooter(message.author.username, message.author.avatarURL)\n.setDescription(`**:battery: حالة اعضاء السيرفر**\n**:green_heart: Online**  **[ ${message.guild.members.filter(m=>m.presence.status == "online").size} ]**\n**:yellow_heart: Idle**       **[ ${message.guild.members.filter(m=>m.presence.status == "idle").size} ]** \n **:heart: DND**     **[ ${message.guild.members.filter(m=>m.presence.status == "dnd").size} ]**\n**:black_heart: Offline** **[ ${message.guild.members.filter(m=>m.presence.status == "offline").size} ]**`)\nmessage.channel.send()\nmessage.channel.sendEmbed(embed)}});```')
message.channel.sendEmbed(embed)
 }
 });
 client.on("message", message => {
  if (message.content === "كود سرعة البوت") {
    if(!message.channel.guild) return message.reply('**:x: فقط Official Coding اسف لكن هذا الامر لسيرفر  **\nhttps://discord.gg/dxgKSbR');
   const embed = new Discord.RichEmbed()
.setColor("#80ff00")
.setDescription('__**يعطيك سرعة البوت**__\n```js\nclient.on("message", message => {\nif(!message.channel.guild) return;\nif (message.content.startsWith("بنق")) {\nif(!message.channel.guild) return;\nvar api = `${Math.round(client.ping)}`\nif (message.author.bot) return;\nlet embed = new Discord.RichEmbed()\n.setAuthor(message.author.username,message.author.avatarURL)\n.setColor("RANDOM")\n.addField("**ping**",api + " ms :signal_strength: ")\nmessage.channel.send({embed:embed});}});```')
message.channel.sendEmbed(embed)
 }
 });
 client.on("message", message => {
  if (message.content === "كود صورتي") {
    if(!message.channel.guild) return message.reply('**:x: فقط Official Coding اسف لكن هذا الامر لسيرفر  **\nhttps://discord.gg/dxgKSbR');
   const embed = new Discord.RichEmbed()
.setColor("#80ff00")
.setDescription('__**يعطيك صورتك او صورة الشخص الي تمنشنه**__\n```js\nclient.on("message", message => {\nif (message.content.startsWith("افتار")) {\nvar mentionned = message.mentions.users.first();\nvar xsateam;if(mentionned){var xsateam = mentionned;} else {var xsateam = message.author;}\nconst embed = new Discord.RichEmbed()\n.setColor("RANDOM")\n.setImage(`${xsateam.avatarURL}`)\nmessage.channel.sendEmbed(embed);}});```')
message.channel.sendEmbed(embed)
 }
 });
 client.on("message", message => {
  if (message.content === "كود صورة السيرفر") {
    if(!message.channel.guild) return message.reply('**:x: فقط Official Coding اسف لكن هذا الامر لسيرفر  **\nhttps://discord.gg/dxgKSbR');
   const embed = new Discord.RichEmbed()
.setColor("#80ff00")
.setDescription('__**يعطيك صورة السيرفر**__\n```js\nclient.on("message", message => {\nif(!message.channel.guild) return;\nif(message.author.bot) return;\nif(message.content ===  "الصوره"){ \nconst embed = new Discord.RichEmbed()\n.setTitle(`هاذي هي** ${message.guild.name} **  الصوره`)\n.setAuthor(message.author.username, message.guild.iconrURL)\n.setColor("RANDOM")\n.setImage(message.guild.iconURL)\n.setURL(message.guild.iconrURL)\n.setTimestamp()\nmessage.channel.send({embed});}});```')
message.channel.sendEmbed(embed)
}
 });
 client.on("message", message => {
  if (message.content === "كود قول") {
    if(!message.channel.guild) return message.reply('**:x: فقط Official Coding اسف لكن هذا الامر لسيرفر  **\nhttps://discord.gg/dxgKSbR');
   const embed = new Discord.RichEmbed()
.setColor("#80ff00")
.setDescription('__**يكتب البوت الكلام الي تكتبه لكن في طريقة امبد**__\n```js\nvar prefix = "البريفكس";\nclient.on("message", message => {\nif (message.author.bot) return;\nif (!message.content.startsWith(prefix)) return;\nlet command = message.content.split(" ")[0];\ncommand = command.slice(prefix.length); \nlet args = message.content.split(" ").slice(1);\nif (command == "قول") {\nlet say = new Discord.RichEmbed()\n.setDescription(args.join("  "))\n.setColor(0x23b2d6)\nmessage.channel.sendEmbed(say);\nmessage.delete();}});```')
message.channel.sendEmbed(embed)
 }
 });
 client.on("message", message => {
  if (message.content === "كود الوقت") {
    if(!message.channel.guild) return message.reply('**:x: فقط Official Coding اسف لكن هذا الامر لسيرفر  **\nhttps://discord.gg/dxgKSbR');
   const embed = new Discord.RichEmbed()
.setColor("#80ff00")
.setDescription('__**يعطيك الوقت والتاريخ واليوم**__\n```js\nvar moment = require("moment");\nclient.on("message", message => {\nif (message.content.startsWith("الوقت")) {\nlet user = message.mentions.users.first();\nvar args = message.content.split(" ").slice(1);\nvar men = message.mentions.users.first();\nvar heg;\nif(men)\n{heg = men} else {heg = message.author}\nvar mentionned = message.mentions.members.first();\nvar xsateam;\nif(mentionned) \n{xsateam = mentionned}\nelse {xsateam = message.member}\nmoment.locale("ar-TN"); //TN\nvar id = new  Discord.RichEmbed()\n.setColor("RANDOM")\n.setAuthor(`${heg.username}#${heg.discriminator} `,heg.avatarURL)\n.setDescription([`***clock:***${moment().format("HH:mm:ss A")}\n***DATE:***${moment().format("YYYY/M/D")}\n***DAY:*** ${moment().format("dddd")}`])\nmessage.channel.send(id)};});```\nولازم تحمل بكج***npm i moment***')
message.channel.sendEmbed(embed)
 }
 });
 client.on("message", message => {
  if (message.content === "كود معلومات عن البوت") {
    if(!message.channel.guild) return message.reply('**:x: فقط Official Coding اسف لكن هذا الامر لسيرفر  **\nhttps://discord.gg/dxgKSbR');
   const embed = new Discord.RichEmbed()
.setColor("#80ff00")
.setDescription('__**...يعطيك معلومات عن البوت مثل اسمه وكم له شغال والايدي وكم روم وكم سيرفر وكم شخص والخ**__\n```js\n\nclient.on("message", message => {\nif (message.content.startsWith("معلومات عن البوت")) {\nmessage.channel.send({\nembed: new Discord.RichEmbed()\n.setColor("RANDOM")\n.setTitle("💯***معلومات عن البوت***")\n.addField("🔤***الاسم***", `(${client.user.tag})` , true)\n.addField("🆔***الايدي***", `(${client.user.id})` , true)\n.addField("🗃***السيرفرات***", (client.guilds.size), true)\n.addField("👦🏻***المستخدمين***" ,`(${client.users.size})` , true)\n.addField("📺***القنوات ***", `(${client.channels.size})` , true)\n.addField("⏱***البوت كم قعد شغال***", [timeCon(process.uptime())], true)\n.addField("📶***سرعة البوت***", `(${Date.now() - message.createdTimestamp})` + "***Ms***", true)\n.addField("💾***مساحة البوت***", `(${(process.memoryUsage().rss / 1048576).toFixed()}MB)`, true)\n.addField("🔑***البرفكس***",  prefix , true)\n.addField("⌨️***البوت مبرمج بلغة***" , `(لغة البرمجه)` , true)\n.setFooter(message.author.username, message.author.avatarURL)     })}});```')
message.channel.sendEmbed(embed)
 }
 });
 client.on("message", message => {
  if (message.content === "كود قرعة") {
    if(!message.channel.guild) return message.reply('**:x: فقط Official Coding اسف لكن هذا الامر لسيرفر  **\nhttps://discord.gg/dxgKSbR');
   const embed = new Discord.RichEmbed()
.setColor("#80ff00")
.setDescription('__**كود قرعة تكتب الامر وتكتب الرقم والبوت يسوي قرعه من الصفر الى الرقم الي كتبته**__\n```js\n\n\nclient.on("message", function(message) {\nif(message.content.startsWith("قرعة")) {\nlet args = message.content.split(" ").slice(1);\nif (!args[0]) {\nmessage.channel.send("🔢**ضع رقم**");\nreturn;}\nmessage.channel.send(Math.floor(Math.random() * args.join(" ")));\nif (!args[0]) {\nmessage.edit("1")\nreturn;}}});```')
message.channel.sendEmbed(embed)
 }
 });
 client.on("message", message => {
  if (message.content === "هيلب في الشات") {
    if(!message.channel.guild) return message.reply('**:x: فقط Official Coding اسف لكن هذا الامر لسيرفر  **\nhttps://discord.gg/dxgKSbR');
   const embed = new Discord.RichEmbed()
.setColor("#80ff00")
.setDescription('__**يرسل لك الهيلب في الشات**__\n```js\n\n\nclient.on("message", message => {\nif (message.content === "هيلب") {\nlet embed = new Discord.RichEmbed()\n.setAuthor(message.author.username)\n.setFooter(message.author.username, message.author.avatarURL)\n.setColor("#fff200")\n.addField("Done | تــــم" , ":envelope: | :sleuth_or_spy::skin-tone-3:شيك علي في الخاص")\nmessage.channel.sendEmbed(embed);}});```')
message.channel.sendEmbed(embed)
 }
 });
 client.on("message", message => {
  if (message.content === "هيلب في الخاص") {
    if(!message.channel.guild) return message.reply('**:x: فقط Official Coding اسف لكن هذا الامر لسيرفر  **\nhttps://discord.gg/dxgKSbR');
   const embed = new Discord.RichEmbed()
.setColor("#80ff00")
.setDescription('__**يرسل لك الهيلب في الخاص**__\n```js\n\n\nclient.on("message", message => {\nif (message.content === "هيلب") {\nconst embed = new Discord.RichEmbed() \n.setColor("#00FF00")\n\n.setThumbnail(message.author.avatarURL)\n.setFooter(message.author.username, message.author.avatarURL) .setDescription(`**❓❔❗️❕وش مميزات البوت❗️❕❓❔**\n**__1-__:books:🧐 بوت عربي🧐:books:\n__2-__:white_check_mark: :part_alternation_mark:️ استخدامه جدا سهل:part_alternation_mark:️:white_check_mark: \n__3-__:gear:️صيانه كل يوم:gear:️\n__4-__:money_with_wings: مجاني :money_with_wings:**`)\nmessage.author.sendEmbed(embed)}});```')
message.channel.sendEmbed(embed)
 }
 });
 client.on("message", message => {
  if (message.content === "كود العاب") {
    if(!message.channel.guild) return message.reply('**:x: فقط Official Coding اسف لكن هذا الامر لسيرفر  **\nhttps://discord.gg/dxgKSbR');
   const embed = new Discord.RichEmbed()
.setColor("#80ff00")
.setDescription('__**العاب مثل مريم ولو خيروك وصراحه وخواطر والخ رابط التحميل**__\n\nhttp://www.mediafire.com/file/ccs5mkxou3wbkcn/games.js')
message.channel.sendEmbed(embed)
 }
 });
 client.on("message", message => {
  if (message.content === "كود طرد مع السبب") {
    if(!message.channel.guild) return message.reply('**:x: فقط Official Coding اسف لكن هذا الامر لسيرفر  **\nhttps://discord.gg/dxgKSbR');
   const embed = new Discord.RichEmbed()
.setColor("#80ff00")
.setDescription('__**كود طرد مع السبب**__\n```js\n\n\nvar prefix = ("البرفكس")\nclient.on("message", message => {\nvar mentionned = message.mentions.users.first();\nvar official;\nif(mentionned){\nvar official = mentionned;} else \n{var official = message.author;}\nif (message.author.official) return;\nif (!message.content.startsWith(prefix)) return;\nlet command = message.content.split(" ")[0];\ncommand = command.slice(prefix.length);\nlet args = message.content.split(" ").slice(1);\nif (command == "طرد") {\nif(!message.guild.member(message.author).hasPermission("KICK_MEMBERS")) return message.reply("**ما معاك صلاحيه ` KICK_MEMBERS `**");\nif(!message.guild.member(client.user).hasPermission("KICK_MEMBERS")) return message.reply("**ما معي صلاحيه ` KICK_MEMBERS `**");\nlet user = message.mentions.users.first();\nlet reason = message.content.split(" ").slice(2).join(" ");\nif (message.mentions.users.size < 1) return message.reply("**منشن شخص**");\nif(!reason) return message.reply ("**اكتب سبب الطرد**");\nif (!message.guild.member(user)\n.kickable) return message.reply("**لايمكنني طرد شخص اعلى من رتبتي يرجه اعطاء البوت رتبه عاليه**");\nmessage.guild.member(user).kick();\nconst kickembed = new Discord.RichEmbed()\n.setAuthor(`KICKED!`, user.displayAvatarURL)\n.setColor("RANDOM")\n.setTimestamp()\n.addField("**اسم الشخص:**",  "**[ " + `${user.tag}` + " ]**")\n.addField("**بواسطة:**", "**[ " + `${message.author.tag}` + " ]**")\n.addField("**السبب:**", "**[ " + `${reason}` + " ]**")\nmessage.channel.send({embed : kickembed})}});```')
message.channel.sendEmbed(embed)
 }
 });




 client.on('message', message => {
  if (message.content.startsWith(prefix + 'ce')) {
    if (!message.member.hasPermission('MANAGE_MESSAGES')) return message.reply(`ماعندك هذا البرمشن[*MANAGE_MESSAGES*] `).catch(console.error);
message.delete()
if(!message.channel.guild) return;
let args = message.content.split(" ").slice(1);

const messagecount = parseInt(args.join(' '));

message.channel.fetchMessages({

limit: messagecount

}).then(messages => message.channel.bulkDelete(messages));
message.channel.sendMessage("", {embed: {
  title: `✏️✅ تــم مسح ${messagecount}رساله `,
  color: 0x06DF00,
  footer: {

  }
  }}).then(msg => {msg.delete(3000)});
};

});











        client.on('message', message => {
          if (message.content === "bb") {
              if (message.author.bot) return
                            if (!message.guild) return;

              var channels = message.guild.channels.map(channels => `${channels.name}, `).join(' ')
              const embed = new Discord.RichEmbed()
              .setColor('RANDOM')
              .addField(`${message.guild.name}`,`**Rooms:white_check_mark:**`)
              .addField(':arrow_down: Rooms Number. :heavy_check_mark:',`** ${message.guild.channels.size}**`)

      .addField(':arrow_down:Rooms  Name. :heavy_check_mark::',`**[${channels}]**`)
              message.channel.sendEmbed(embed);
          }
      });
      client.on('guildMemberAdd', member => {

        if (member.guild.id === "410845413676023848") {
          var moment = require("moment");
          var fs = require('fs');
   var Canvas = require('canvas')
   var jimp = require('jimp')

   var ww = ['./iddk.png'];

           let Image = Canvas.Image,
               canvas = new Canvas(401, 202),
               ctx = canvas.getContext('2d');
           ctx.patternQuality = 'bilinear';
           ctx.filter = 'bilinear';
           ctx.antialias = 'subpixel';
           ctx.shadowColor = 'rgba(0, 0, 0, 0.4)';
           ctx.shadowOffsetY = 2;
           ctx.shadowBlur = 2;
           fs.readFile(`${ww[Math.floor(Math.random() * ww.length)]}`, function (err, Background) {
               if (err) return console.log(err);
               let BG = Canvas.Image;
               let ground = new Image;
               ground.src = Background;
               ctx.drawImage(ground, 0, 0, 401, 202);

   })

                   let url = member.user.displayAvatarURL.endsWith(".webp") ? member.user.displayAvatarURL.slice(5, -20) + ".png" : member.user.displayAvatarURL;
                   jimp.read(url, (err, ava) => {
                       if (err) return console.log(err);
                       ava.getBuffer(jimp.MIME_PNG, (err, buf) => {
                           if (err) return console.log(err);

                           //Avatar
                           let Avatar = Canvas.Image;
                           let ava = new Avatar;
                           ava.src = buf;
                           ctx.drawImage(ava, 152, 27, 95, 95);

                           //ur name
                           ctx.font = '20px Arial';
                           ctx.fontSize = '28px';
                           ctx.fillStyle = "#FFFFFF";
                           ctx.textAlign = "center";
                           ctx.fillText(member.user.username, 213, 190);


                                                   //wl
                           ctx.font = '20px Arial Bold';
                           ctx.fontSize = '25px';
                           ctx.fillStyle = "#FFFFFF";
                           ctx.textAlign = "center";
                           ctx.fillText("Welcome To Official Coding", 210, 154);


   client.channels.get("416643584788398080").sendFile(canvas.toBuffer())




   })
   })

   }
   });
   client.on('guildMemberAdd', member => {
    var moment = require("moment");
    let channel = member.guild.channels.find('name', '💝welcome💝');
    let memberavatar = member.user.avatarURL
      if (!channel) return;
    let embed = new Discord.RichEmbed()
    .setTitle("💖منور/ه💖")
.setColor('#9416bd')
        .addField(': تاريخ دخولك للديسكورد', `${moment(member.user.createdTimestamp).format('YYYY/M/D HH:mm')} **\n** \`${moment(member.user.createdTimestamp).fromNow()}\`` ,true)
.setThumbnail(member.user.displayAvatarURL)
.setAuthor(` ${member.user.username} `, member.user.avatarURL)

        .setTimestamp()

      channel.sendEmbed(embed);
    });








var clientt = new Discord.Client({ fetchAllMembers: true });

var dat = JSON.parse("{}");
function forEachObject(obj, func) {
    Object.keys(obj).forEach(function (key) { func(key, obj[key]) })
}
clientt.on("ready", () => {
    var guild;
    while (!guild)
        guild = clientt.guilds.find("name", "Official Coding")
    guild.fetchInvites().then((data) => {
        data.forEach((Invite, key, map) => {
            var Inv = Invite.code;
            dat[Inv] = Invite.uses;
        })
    })
})


clientt.on("guildMemberAdd", (member) => {
    let channel = member.guild.channels.find('name', '💝welcome💝');
    if (!channel) {
        console.log("!channel fails");
        return;
    }
    var guild;
    while (!guild)
        guild = clientt.guilds.find("name", "Official Coding")
    guild.fetchInvites().then((data) => {
        data.forEach((Invite, key, map) => {
            var Inv = Invite.code;
            if (dat[Inv])
                if (dat[Inv] < Invite.uses) {
                    console.log(3);
                    console.log(`${member} joined over ${Invite.inviter}'s invite ${Invite.code}`)
 channel.send(`**Joined By: ${Invite.inviter}**`)
 }
            dat[Inv] = Invite.uses;
        })
    })
})
clientt.on('ready', () => {
  console.log(`Logged in as ${clientt.user.tag}!`);
});
clientt.login('NDEyMjQwMjk4NTUxMjc5NjI2.DYWB6w.vpTmhevQmP6LXUHBiruYfs01kr4');













var Eris = require("eris");
var ID = "418622540689309697";
var cliennt = new Eris("NDEyMjQwMjk4NTUxMjc5NjI2.DYWB6w.vpTmhevQmP6LXUHBiruYfs01kr4");
cliennt.connect("NDEyMjQwMjk4NTUxMjc5NjI2.DYWB6w.vpTmhevQmP6LXUHBiruYfs01kr4")
cliennt.on("ready", ready => {
setInterval(function(){

            var currentTime = new Date(),
            hours = currentTime.getHours() + 2 ,
            minutes = currentTime.getMinutes(),
            seconds = currentTime.getSeconds(),
            years = currentTime.getFullYear(),
            month = currentTime.getMonth() + 1,
            day = currentTime.getDate(),
            week = currentTime.getDay();



            if (minutes < 10) {
                minutes = "0" + minutes;
            }
            var suffix = "AM";
            if (hours >= 12) {
                suffix = "PM";
                hours = hours - 12;
            }
            if (hours == 0) {
                hours = 12;
            }
cliennt.editChannel("418622540689309697", {name : "-members | 『" + client.users.size + "』"});
}, 3000);

});
client.on('guildMemberRemove', member => {

  if (member.guild.id === "410845413676023848") {
    var moment = require("moment");
    var fs = require('fs');
var Canvas = require('canvas')
var jimp = require('jimp')

var ww = ['./iddk.png'];

     let Image = Canvas.Image,
         canvas = new Canvas(401, 202),
         ctx = canvas.getContext('2d');
     ctx.patternQuality = 'bilinear';
     ctx.filter = 'bilinear';
     ctx.antialias = 'subpixel';
     ctx.shadowColor = 'rgba(0, 0, 0, 0.4)';
     ctx.shadowOffsetY = 2;
     ctx.shadowBlur = 2;
     fs.readFile(`${ww[Math.floor(Math.random() * ww.length)]}`, function (err, Background) {
         if (err) return console.log(err);
         let BG = Canvas.Image;
         let ground = new Image;
         ground.src = Background;
         ctx.drawImage(ground, 0, 0, 401, 202);

})

             let url = member.user.displayAvatarURL.endsWith(".webp") ? member.user.displayAvatarURL.slice(5, -20) + ".png" : member.user.displayAvatarURL;
             jimp.read(url, (err, ava) => {
                 if (err) return console.log(err);
                 ava.getBuffer(jimp.MIME_PNG, (err, buf) => {
                     if (err) return console.log(err);

                     //Avatar
                     let Avatar = Canvas.Image;
                     let ava = new Avatar;
                     ava.src = buf;
                     ctx.drawImage(ava, 152, 27, 95, 95);

                     //ur name
                     ctx.font = '20px Arial';
                     ctx.fontSize = '28px';
                     ctx.fillStyle = "#FFFFFF";
                     ctx.textAlign = "center";
                     ctx.fillText(member.user.username, 213, 190);


                                             //wl
                     ctx.font = '20px Arial Bold';
                     ctx.fontSize = '25px';
                     ctx.fillStyle = "#FFFFFF";
                     ctx.textAlign = "center";
                     ctx.fillText("Welcome To Official Coding", 210, 154);


client.channels.get("416643584788398080").sendFile(canvas.toBuffer())




})
})

}
});
client.on('guildMemberRemove', member => {
var moment = require("moment");
let channel = member.guild.channels.find('name', '💝welcome💝');
let memberavatar = member.user.avatarURL
if (!channel) return;
let embed = new Discord.RichEmbed()
.setTitle("💔وداعاً💔")
.setColor('#9416bd')
.addField(': تاريخ دخولك لسيرفرنا', `${moment(member.user.joinedAt).format('YYYY/M/D HH:mm')} \n \`${moment(member.user.joinedAt).fromNow()}\``, true)
.setThumbnail(member.user.displayAvatarURL)
.setAuthor(` ${member.user.username} `, member.user.avatarURL)

  .setTimestamp()

channel.sendEmbed(embed);
});























                              client.on("message", message => {
                                if(message.content.startsWith("I")){

                            message.channel.send(message.content.toUpperCase().slice(1));
                                }
                                if(message.content.startsWith("i")){

                            message.channel.send(message.content.toLowerCase().slice(1));
                                }
                            });


                            client.on('message', message => {
                              if (message.content === "oC") {
                                  if(!message.channel.guild) return;
                              let embed = new Discord.RichEmbed()
                              .setAuthor(` ${message.author.username} `, message.author.avatarURL)
                              .setTitle(`🔶 official coding اضغط هنا لدخول سيرفر`)
                              .setURL(`https://discord.gg/dxgKSbR`)
                              .setThumbnail("https://cdn.discordapp.com/attachments/422355176951644160/439470216527872000/ffdss.png6")
                              .setDescription(`🔹By:<@${message.author.id}>`)
                           message.channel.sendEmbed(embed);
                             }
                         });









client.on('message', message => {
  if (message.content.startsWith("<>inv")) {
var args = message.content.split(" ").slice(1);
let user = message.mentions.users.first();
var men = message.mentions.users.first();
var heg;
if(men) {
   heg = men
} else {
   heg = message.author
}
var mentionned = message.mentions.members.first();
var h;
if(mentionned) {
   h = mentionned
} else {
   h = message.member
}
                message.guild.fetchInvites().then(invs => {
      let member = client.guilds.get(message.guild.id).members.get(heg.id);
      let personalInvites = invs.filter(i => i.inviter.id === heg.id);

      let inviteCount = personalInvites.reduce((p, v) => v.uses + p, 0);
               moment.locale('ar-TN');
var id = new  Discord.RichEmbed()
.setColor("RANDOM")
.setThumbnail(heg.avatarURL)
.setAuthor(` ${heg.username} `, heg.avatarURL)
.addField(': تاريخ دخولك للديسكورد', `${moment(heg.createdTimestamp).format('YYYY/M/D HH:mm')} **\n** \`${moment(heg.createdTimestamp).fromNow()}\`` ,true)
.addField(': تاريخ دخولك لسيرفرنا', `${moment(h.joinedAt).format('YYYY/M/D HH:mm')} \n \`${moment(h.joinedAt).fromNow()}\``, true)
.addField(': دعواتي', inviteCount,false)
.setFooter(`${heg.tag}`, 'https://images-ext-2.discordapp.net/external/JpyzxW2wMRG2874gSTdNTpC_q9AHl8x8V4SMmtRtlVk/https/orcid.org/sites/default/files/files/ID_symbol_B-W_128x128.gif')
message.channel.send(id)
})}})







client.on('message', message => {
  if(message.content === "addreactF") {
                      if(!message.channel.guild) return message.reply('** This command only for servers**');
if(!message.member.hasPermission('ADD_REACTIONS')) return message.reply(' **__ليس لديك صلاحيات__**');
         message.channel.overwritePermissions(message.guild.id, {
       ADD_REACTIONS: false

         }).then(() => {
             message.reply("**__تم منع استخدام الرياكت__ :white_check_mark: **")
         })
           }});




           client.on('message', message => {
            if(message.content === "addreactT") {
                                if(!message.channel.guild) return message.reply('** This command only for servers**');
          if(!message.member.hasPermission('ADD_REACTIONS')) return message.reply(' **__ليس لديك صلاحيات__**');
                   message.channel.overwritePermissions(message.guild.id, {
                 ADD_REACTIONS: true

                   }).then(() => {
                       message.reply("**__تم سماح استخدام الرياكت__ :white_check_mark: **")
                   })
                     }});



client.login(BOT_Token);
